Create the file handling objects


Print a user friendly message informing user their file is being saved


Get user input for what the file name will be, store in variable

Instantiate the file handling objects and concatenate the user input to name the file

open file ("data/" + file name + ".txt")

The first line of the file will be the number of cars that the file contains, print number and move to the next line in the file

Save the rest of the contents of the array to the file
for i = 0 to array - 1
  Write each line to the file, and move to the next line each time through the loop

Close the file
